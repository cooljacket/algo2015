#include <stdio.h>
#include <algorithm>
using namespace std;

const int MAX = 20005;
int dragon[MAX], loowater[MAX];

int main() {
	int n, m;
	while (scanf("%d%d", &n, &m) == 2 && n != 0 && m != 0) {
		
	}
	
	return 0;
}